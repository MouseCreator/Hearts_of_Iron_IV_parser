package mouse.hoi.main.common.tester;



public interface TokenTester {
    boolean isCountryTag(String string);
    boolean isInteger(String string);
    boolean isEffect(String stringKey);
    boolean isTrigger(String stringKey);
}
